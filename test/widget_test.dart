import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should render bookshelf page', (WidgetTester tester) async {
    // Basic smoke test
    // await tester.pumpWidget(const EasyReadApp());
    // expect(find.text('书架'), findsOneWidget);
  });
}
