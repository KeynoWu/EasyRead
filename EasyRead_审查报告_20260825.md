# EasyRead 完整审查报告（2026-08-25）

工具基线：`flutter analyze` 零告警；`flutter test` 363 全过。
方法：5 路并行分片审查（core / reader / book_source+search / bookshelf+settings+shell / 安全跨切面），主代理对关键发现逐条复验。已剔除误报：「switch 缺 break 编译错误」（Dart 3 case 隐式 break，analyze 通过）。

标记说明：【已复验】= 主代理对照代码逐条确认；其余为审查代理报告且附代码证据。

---

## 高严重度（7）

### H1. 旧式索引 `step=0` 死循环冻结主 isolate【已复验】
- 位置：`lib/features/search/data/engines/rule_parser.dart:309-327`（`parseLegacyIndexes`）→ `selector_engine.dart:528`（`expandIndexes` else 分支）
- 方括号语法 `[5:0:0]` 已在 `parseIndexSet`（rule_parser.dart:349）拒绝 `step==0`，但旧式 `.`/`!` 语法（`tag.li.5:0:0`、`children.2:0:0`）无此防护。规则含 `start:end:0` 且 `start>=end` 时，`for (i=start; i>=end; i+=0)` 死循环冻结主 isolate。
- 死循环组合仅 `step==0 且 start>=end`（负步长严格递减必终止，`start<end` 时 else 分支初始条件为假直接返回空，均不构成死循环）。
- 规则字符串来自用户导入的书源 JSON，完全可控。
- 修复：`parseLegacyIndexes` 补 `if (step == 0) return null;`（与 parseIndexSet 对齐）。IndexRange 全库仅 legacy/bracket 两处构造，补上后 step==0 不可达 expandIndexes，无需额外防御。

### H2. 图片节点分页测量 ≠ 渲染，带图章节整页错位【已复验】
- 位置：`page_layout.dart:133-135`（测量预留 40~200px）vs `page_view_widget.dart:454`、`scroll_view_widget.dart:279`（渲染返回 `SizedBox.shrink()`，高度 0）
- 每个图片节点留一块空白，翻页次数虚增、翻页跳位、pageIndex 与用户所见漂移。`imageUrl` 已解析但被渲染层丢弃。
- 修复：二选一——渲染层真正渲染图片（加载后按实际高度重分页）；或测量与渲染统一为 0 高度（暂不支持图片）。

### H3. 目录点「第 1 章」不跳转，打开的是旧进度【已复验】
- 位置：`book_detail_page.dart:126` 仅 `chapterIndex > 0` 才 `saveProgress`；阅读器起始章节完全来自进度（`reader_page.dart:96-100`）
- 点第 5 章正常，点第 1 章却续读旧位置。已读书想从头重读必然踩中。
- 修复：点击任意章节（含 0）都写入进度再进入阅读器。

### H4. JsNetwork 同站拦截用 `return`，worker 队列剩余 URL 全部静默丢弃【已复验】
- 位置：`js_network.dart:38`（fetchAll）、`:130`（fetchNetworkResults）
- `isSameSite` 失败应跳过本条却退出整个 worker，队列剩余 URL 不再被抓取 → JS 侧拿到空串。
- 修复：两处 `return` → `continue`。

### H5. Hive 加密盒打开失败击穿启动，无恢复入口
- 位置：`hive_init.dart:46/94-97` + `main.dart:8`
- 密钥不匹配或盒文件损坏时 `crashRecovery:false` 使 Hive 抛 `HiveError`，`initHive` 无 try/catch → 启动即崩/白屏，与注释宣称的「降级可用」语义不符。写盘崩溃一次即进入启动崩溃循环。
- 修复：捕获 HiveError 区分「密钥不匹配」与「文件损坏」；启动失败给可读错误页；损坏盒备份后重建或启用 crashRecovery。

### H6. quickjs 无中断处理器：JS 死循环超时后原生线程继续烧 CPU
- 位置：`third_party/quickjs/lib/src/native_js_engine.dart:212-220`（只设内存 64MB/栈 1MB）；`JS_SetInterruptHandler`（lib_quickjs.dart:1221）从未调用
- `while(true){}` 书源规则触发 3s 超时后 `_isolate.kill` 无法抢占同步 FFI（JS_Eval）中的原生线程 → 占满一个核，反复触发可耗尽 CPU。
- 修复：注册计数式 `JS_SetInterruptHandler` 中断回调，超限让 JS_Eval 以 interrupted 异常返回。

### H7. Release 实际用全网公开的 debug keystore 签名【已复验】
- 位置：`android/keystore.properties` 指向 `~/.android/debug.keystore`（别名 `androiddebugkey`）+ `android/app/build.gradle.kts:36-62`
- 文件未被 git 跟踪（已验证 `git ls-files`），无泄漏；但按此配置的发布包任何人都可用同证书伪造「更新」。
- 修复：分发前生成独立 release keystore（越早换代价越小，换证书后老用户无法直接升级）。

---

## 中严重度（12）

### M1. `evalTemplate` 绕过 JS 黑名单，可注入 `_ffiNotify` 崩掉引擎 isolate
- `js_rule_executor.dart:395-490` 全程无 `unsupported()` 检查（`execute()` :124 有）；`_ffiNotify` 回调 `json.decode` 无 try-catch；黑名单不解码 `\uXXXX` 转义可绕过。`createEngine` 无超时，isolate 死后 `_sendWaitFor` 永久挂起搜索。

### M2. JS 两遍执行状态污染
- `js_rule_executor.dart:158/204` + `js_bridge.dart:170-179`：`__putMap` 第二遍不重置、crypto 缓存按第一遍（占位）参数建表 → 第二遍新参数静默 miss 返回 `''`。

### M3. record-replay 控制流依赖 `java.get` 结果时取值表错位
- `js_bridge.dart:228/283` + `js_record_replay.dart:54`：记录遍 get 恒返回 `''`，分支走向与最终遍不同 → 值表顺序消费全部偏移，失败静默。

### M4. 分组对话框「取消」=「不分组」，误清书籍分组【已复验】
- `bookshelf_page.dart:329-349`：取消与「不分组」都返回 null 且无条件 `setGroup(id, null)` → 静默数据丢失。修复：哨兵值区分。

### M5. `_refreshAllBooks` 无 finally 复位
- `bookshelf_page.dart:209-227`：中途异常后 `_refreshingAll` 保持 true，刷新按钮永久禁用。

### M6. 空闲超时覆盖连接/重试阶段，慢网络误报「下载中断」
- `import_book_source.dart:121-142`：重试期间 `lastActivity` 不刷新，单次重试即 21s > 20s 空闲窗口。修复：空闲窗口从首字节到达起算。

### M7. Dart 正则路径无超时兜底 + 一处 ReDoS guard 遗漏
- `regex_purifier.dart:90-104` 主 isolate 同步执行无超时；guard 只查指数型不查多项式回溯；`content_extractor.dart:213-216` 的 `replaceRegex` 连 guard 都没有（同类三处都有）。

### M8. 21MB 书源 JSON 在主 isolate 全量解析 + 逐项 re-encode/re-decode
- `import_book_source.dart:228-239`：阻塞 UI 数秒、内存峰值 2-3 倍。修复：下沉 isolate + 直接走 Map 分支。

### M9. 书源登录 WebView 无 scheme 白名单
- `book_source_login_page.dart:18-34`：`loginUrl` 为 `file:///...` 绝对 URL 时 resolve 原样放行加载（javaScriptEnabled:true）。修复：最终 scheme 仅放行 http/https。

### M10. 阅读进度无 sourceId，换源后按旧索引续读错章
- `ReadingProgress` 实体缺 sourceId 维度；不同源目录结构不同，旧索引指向错误章节甚至越界连锁触发自动换源。

### M11. 分页缓存 key 不含简繁模式 + `setChineseMode` 竞态
- `reader_provider.dart:564-615`：加载中切简繁 → seq 提前 return 缓存未清 → 翻页/滚动两种模式显示不同简繁内容。

### M12. POST 重试重发请求体
- `retry_interceptor.dart:28-35`：响应超时重试产生重复副作用；4 次×10s 重试栈加剧 M6 误杀。

---

## 低严重度（摘选 16）

- L1. **整本缓存淘汰 `updatedAt` 类型误判**【已复验】：`book_cache_service.dart:129` 写入 `DateTime`，`:183` 只认 `int` → 全部按 epoch 0 排序，淘汰顺序任意，可能误删最近缓存的书。
- L2. 设置面板滑块闭包捕获 build 时快照，快速连调互相覆盖（`reader_settings_panel.dart` 多处）。
- L3. 净化规则变更不失效已缓存章节（`reader_repository_impl.dart:313-318` 缓存 key 无净化指纹）。
- L4. 菜单显隐触发整章重分页且页码 clamp 不按比例换算，阅读位置漂移（`page_view_widget.dart:183-201` + `reader_provider.dart:645-666`）。
- L5. 全书进度滑块 `onChanged` 即跳章，拖动一次触发数十次章节加载（`reader_page.dart:438-445`）。
- L6. 「清除本书缓存」不清 `book_cache` 整本缓存盒，与删除路径不一致（`bookshelf_page.dart:230-234`）。
- L7. SSRF DNS 校验 TOCTOU（重绑定未真封堵）+ IPv6 黑名单缺 `fec0::/10`（`dio_client.dart:269-302/395-414`）。
- L8. 导入页两处 `debugPrint` 未脱敏 URL/完整规则（`book_source_import_page.dart:103/162`）。
- L9. iOS `NSAllowsArbitraryLoads=true` 且含与书源无关的 `NSAllowsLocalNetworking`（`Info.plist:5-10`）。
- L10. 搜索：无详情 URL 时 bookId 用 index 回退跨页碰撞（`search_repository_impl.dart:713-717`）；流式 alternatives 未按 sourceId 去重（`search_books.dart:289-294`）。
- L11. `importFromJson` 空 id 重编号分支 n 不递增（`manage_purification_rules.dart:121-131`）。
- L12. 空闲超时裸 `TimeoutException` 文案不友好（`import_book_source.dart:140`，应抛 ImportLimitExceeded）。
- L13. fetchAll 4 worker 被限频器 'default' 队列串行化 + 8s 超时覆盖排队时间（`js_network.dart:20-40` + `rate_limit_interceptor.dart:16`）。
- L14. `_isPlainBoxOnDisk` 启动时全量读入盒文件（`hive_init.dart:111-113`，只需首帧）。
- L15. 无全局未捕获异常处理（`main.dart` 无 `PlatformDispatcher.instance.onError`/`runZonedGuarded`）。
- L16. 阅读器细节：目录被 `getBookDetail` 与 `_load` 重复拉取；TextPainter 未 dispose 且未应用系统 textScaler；lineBreak 测量/渲染高度不一致；分页在 UI 线程同步执行超大章节卡顿；`_syncBookToShelf` 目录长度执行时读取；退出时进度双写；`_viewportReported` 注释与实现矛盾；切章时 PageView itemCount 收缩短暂越界。

---

## 已确认无问题的面

- Controller/Timer/AnimationController dispose、`_loadSeq` 切章竞态防护、正则零宽匹配防护、HTML 帧栈解析防栈溢出。
- 网络安全层整体扎实：SSRF 私网/保留地址拦截、重定向逐跳校验与敏感头剥离、`redactUrl` 日志脱敏（仅导入页两处遗漏）。
- quickjs 沙箱无 std/os 模块、无文件/网络直达、64MB 内存上限；CookieJar 加密存储（AES-256 + flutter_secure_storage）实现严谨。
- `java.ajax` 同站限制 + `maxFetchUrls=50` 有效封堵 Cookie 外带与请求量失控。

- 第一批（已修）：H1 step==0 防护（parseLegacyIndexes 单点，IndexRange 仅两处构造、补后不可达）、H3 首章进度、H4 return→continue、M4 分组哨兵、M5 finally 复位、L1 缓存淘汰类型兼容。
- 第二批（已修）：H5 启动兜底（加密盒 CRC 预检+备份/截断恢复、main 错误页与全局异常处理）、M6 空闲窗口从首字节起算 + 首字节超时、L12 空闲超时改 ImportLimitExceeded、M1 模板黑名单/`_ffiNotify` 容错/createEngine 超时与 isolate 死亡快速失败、M2 putMap 重置 + crypto 单轮热身重录、M3 record-replay 序列一致性降级。
- 第三批（未做）：H2 图片渲染策略、H6 quickjs 中断处理器、H7 换 release 签名、M7 ReDoS 超时兜底、M8 书源 JSON isolate 化、M9 登录 WebView scheme 白名单、M10 进度带 sourceId、M11 分页缓存 key 加 chineseMode、M12 POST 重试策略、其余低项。

- 第一批（本次已修）：H1 step==0 防护、H3 首章进度、H4 return→continue、M4 分组哨兵、M5 finally 复位、L1 缓存淘汰类型。
- 第二批（稳定性）：H5 启动兜底 + 全局异常处理、M6 空闲窗口从首字节起算、M1/M2/M3 JS 引擎三遍一致性。
- 第三批（体验与安全加固）：H2 图片渲染策略、H6 中断处理器、H7 换 release 签名、M7-M11。
