# P0 实施计划（2026-09-03，依据 .research/00_学习路线图.md）

> 目标：修复 13 项 P0 语义错误，全部带回归测试，`flutter analyze` 0 + `flutter test` 全绿。
> 原则：每项先读 Legado 参照语义（tmp/legado-master），修 EasyRead 实现 + 补单测；不动 GPL 代码。
> 分 4 批，批内同文件合并改；每批结束跑 analyze + test。

## Batch 1：解析器/净化（2 项）
- **P0-1 legacy 索引 `:` 语义反转**：`tag.div!0:3` / `tag.div.0:3` 应=离散索引集 {0,3}（Legado AnalyzeByJSoup.kt:283,491-501），现被解析为范围 {0,1,2}。
  - 改：rule_parser.dart `parseLegacyIndexes` — `:` 拆分为离散列表（与 `[a,b,c]` 同语义）；`!` 前缀排除。
  - 注意：新式 `[a:b:c]` 范围语法保持不变（parseIndexSet 不动）。selector_engine expandIndexes 不需改（消费 IndexRange）。
  - 测试：rule_engine_test / rule_engine_cascade_test 补 4 例（离散、排除、混合、负数）。
- **P0-13 净化 `$N` 替换双路径不一致**：Dart 路径（regex_purifier.dart:169-173）字面替换 `$1`，应展开捕获组（对齐 Legado RegexExtensions.kt:43 的 appendReplacement 语义：$1..$9、$& 整匹配、`$$` 转义）。
  - 测试：regex_purifier_test 补 3 例（$1、$&、$$ 字面量、越界 $9=空）。

## Batch 2：JS 桥语义（6 项，同文件族）
- **P0-2 java.get 重定义**：1 参=变量读取（`__putMap` + 注入的 variables + 特殊键 bookName/title/sourceUrl/key）；2 参(url,headers)=同站 GET 无重定向。getElement 返回 Element 风格对象（html/text/attr 方法）。（js_bridge.dart:197/262/318；L: AnalyzeRule.kt:715,320; JsExtensions.kt:359）
- **P0-3 variables 注入**：execute() 时把 Dart 侧 variables 合入 `__putMap` 初始表（js_rule_executor.dart；L: AnalyzeRule.kt:704-730）
- **P0-4 java.post 返回 body**（js_network.dart:135-159；L: JsExtensions.kt:401）
- **P0-5 ajax/post 错误返回 `Exception: <msg>` 字符串**（js_network.dart:47/162；L: AnalyzeRule.kt:783 stackTraceStr）
- **P0-6 md5Encode16 = 中段 16 位 substring(8,24)**（js_bridge.dart:741；L: MD5Utils.kt:22-25）
- **P0-7 timeFormat 1 参默认 "yyyy/MM/dd HH:mm" 本地时区**（js_crypto.dart:447-461；L: JsExtensions.kt:504; AppConst.kt:39-41）
- 测试：js_rule_executor_test / js_template_test / 新增 js_bridge_get_test。

## Batch 3：阅读链（3 项）
- **P0-8 ruleContent.replaceRegex 完整规则语义**：不认 ["pat","rep"] 简写以外的整规则（纯 `##` 规则=只替换不提取；作用于全文）；兼容旧 ["pat","rep"] 存量。（content_extractor.dart:185-207；L: BookContent.kt:138-143; AnalyzeRule.kt:279）
- **P0-9 nextContentUrl 跨章守卫**：next==下一章 URL 即停；顺带检查 20 页上限保留。（reader_repository_impl.dart:414-445；L: BookContent.kt:47-52,83-86）
- **P0-10 目录 `chapterList -/+` 前缀 + 章节 URL 空兜底**：剥前缀（- 倒序）；chapterUrl 空→卷=标题+序号、普通=baseUrl#序号（L: BookChapterList.kt:49-56,230-244）。（catalog_parser.dart:133-143,127）
- 测试：content_extractor / reader_repository 相关 + catalog_parser 测试。

## Batch 4：基建（2 项）
- **P0-11 Set-Cookie 全局回写**：dio 拦截器对所有响应（含重定向逐跳）按名合并回写 cookie 盒（按 sourceId 上下文）；JS 网络注入存储 Cookie。（cookie_jar_service.dart / dio_client.dart / js_network.dart:132-145；L: CookieManager.kt:41-50; CookieStore.kt:34-47）
  - 设计：CookieJarService 增加 `absorbFromResponse(Uri, headers)`；DioClient 在有 sourceId 上下文时调用。注意不破坏现有 AES 盒。
- **P0-12 书源导入 lastUpdateTime 冲突**：模型补字段（默认 0）；导入时已存在且 lastUpdateTime 更新者胜、保留本地 enabled/group/自定义变量；新增 lastUpdateTime 变化时才覆盖规则。（import_book_source.dart:71-73; book_source.dart；L: ImportBookSourceViewModel.kt:85-128,207-218）
- 测试：cookie_jar_service_test / import_book_source_test。

## 验收
- 每批：`flutter analyze` 0 issues + `flutter test` 全绿（基线 417）。
- 全部完成后：真实源 e2e（integration_test/real_source_e2e_test.dart）macOS 跑通（可选）。
- 提交：工作区另有历史 WIP（M-A 文法），完成后由用户决定是否分 commit。

## 后续轮次（P1）
按路线图 §二（文法/引擎 15 项）→ §三（体验 12 项）顺序推进，每项独立小批 + 测试。
